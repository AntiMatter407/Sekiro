---
name: breakdown
description: "需求拆分与追踪工具。将需求智能拆分为可执行的子任务树，生成结构化文档并持续追踪进度。"
---

# /breakdown — 需求拆分与追踪

## 触发方式

| 命令 | 作用 |
|------|------|
| `/breakdown <需求描述>` | 创建新需求文档，智能拆分并输出任务树 |
| `/breakdown list` | 列出 `Docs/breakdown/` 中所有需求及状态 |
| `/breakdown <名称>` | 打开指定需求文档，恢复/继续/修改该需求 |

`<名称>` 匹配 `Docs/breakdown/` 中的文件名（不含 `.md`），支持模糊匹配。

## 文档模板

新建需求时，在 `Docs/breakdown/<slug>.md` 创建：

```markdown
# [需求名称]

| 状态 | 创建 | 更新 |
|------|------|------|
| 🔴 未开始 | YYYY-MM-DD | YYYY-MM-DD |

## 需求描述
（用户原始需求 + AI 理解确认）

## 任务树
<!--
  状态图标：⬜ 未开始  🔄 进行中  ✅ 已完成  ❌ 已取消
  依赖标注：任务后追加 (依赖: 1, 2.1)
-->

- ⬜ 1. 子任务名称
  - ⬜ 1.1 子子任务
  - ⬜ 1.2 子子任务 (依赖: 1.1)
- ⬜ 2. 子任务名称 (依赖: 1)

## 变更记录
| 日期 | 变更 |
|------|------|
| YYYY-MM-DD | 创建 |
```

## 拆分原则

1. **先理解再拆分** — 读取 `.claude/docs/directory-structure.md` 和 `CLAUDE.md` 获取项目上下文
2. **自顶向下** — 先拆顶层任务，用户确认后再递归拆分子任务
3. **依赖明确** — 每个任务标注依赖关系，形成 DAG
4. **可验证** — 每个叶子任务有明确完成标准

## 确认机制

1. AI 分析需求后，**先在对话中输出任务树草稿**
2. 用户可以增删改任意任务、调整依赖、修改描述
3. 用户确认（"OK / 写入 / 下一步"）后，AI 写入 `Docs/breakdown/<slug>.md`
4. 后续修改任务时，同样先展示变更 → 确认 → 再写入
5. 递归拆分时，每层都需要确认

## 操作规则

- **每次操作后**自动更新文档的「更新」日期和「变更记录」
- **状态切换**：⬜ → 🔄（开始）→ ✅（完成）/ ❌（取消）
- **任务变更**：增删改任务时，在变更记录中注明
- **完成不删除**：状态改为 🟢 已完成，文档保留
- **继续旧需求**：通过 `/breakdown <名称>` 打开，可在原文档上修改、追加、重启

## 执行规范

### `/breakdown list` 固定步骤

```
1. Glob path=".claude/docs/breakdown" pattern="**/*.md"
2. 遍历结果，用 Read 读取每个文件头几行提取状态
3. 按格式输出
```

### `/breakdown <名称>` 固定步骤

```
1. Glob path=".claude/docs/breakdown" pattern="{名称}.md"
   未命中时模糊匹配：Glob path=".claude/docs/breakdown" pattern="**/*.md"，找名称相似的文件
2. Read 匹配文件，输出任务树和状态
```

### 写入路径

所有 breakdown 文档统一写入 `Docs/breakdown/`。